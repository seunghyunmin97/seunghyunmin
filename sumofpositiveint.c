#include <stdio.h>

int main()
{
int n, i, cnt=0;
int sum=0;
//int arr[5];

for(i=0;cnt<5;i++){
  printf("정수 >> ")
  scangf("%d", &n);

  if (n<=0)  continue;

  cnt++;

}

/*
for(int i=0;i<5;)
  {
    printf("\n양의 정수 n을 입력하세요. ");
    scanf("%d",&n);

    if(n>0)
    {
      printf("지금까지의 합: %d + %d = ",sum,n);
      sum += n;
      printf("%d\n",sum);
      i++;
    }

    else {
      printf("양의 정수만 입력가능\n\n");

    }

}
*/

  return 0;
}
